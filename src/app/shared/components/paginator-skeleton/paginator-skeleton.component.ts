import { Component } from '@angular/core';

/**
 * Componente visual de carga (*Skeleton Loader*) para el paginador.
 * 
 * Muestra la silueta animada del control de paginación mientras se resuelve 
 * la obtención asíncrona de datos desde la API.
 */
@Component({
  selector: 'paginator-skeleton',
  templateUrl: './paginator-skeleton.component.html',
  styleUrls: ['./paginator-skeleton.component.css']
})
export class PaginatorSkeletonComponent {

}